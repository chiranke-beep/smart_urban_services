# Database (Connects to your real local PostgreSQL 18 database)
DB_HOST=host.docker.internal
DB_PORT=5432
DB_NAME=smart_urban_services
DB_USER=postgres
DB_PASSWORD=chiran123

# JWT
JWT_SECRET=smart_urban_services_jwt_super_secret_key_2026

# Frontend URL (update when deployed to AWS)
CLIENT_URL=http://localhost:3000

# Backend URL (used by Next.js at build time)
NEXT_PUBLIC_API_URL=http://localhost:5000
